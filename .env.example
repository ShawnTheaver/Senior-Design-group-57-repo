# Example environment variables (no secrets)
NEXT_PUBLIC_APP_NAME=CatAssist
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# Add other non-sensitive keys here if needed later
# e.g. NEXT_PUBLIC_API_URL=https://api.example.com
